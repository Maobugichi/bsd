import { Settings, Building2, UserCheck, CircuitBoard } from "lucide-react";

export const icons = {
  settings: Settings,
  building2: Building2,
  userCheck: UserCheck,
  circuitBoard: CircuitBoard,
} as const;
